pipeline {
    agent any

    stages {
        stage ('build'){
                steps {
                sh 'echo "witoutdocker"'
                sh 'npm --version'
                echo "hello panda"
                
            }
        }
    }
}
